#include <stdio.h>

int findNb(int volume) {
        
}

int main(int argc, char*[] args) {
        int volume = 0;
        int cubes = 0;

        printf("Please enter a volume: ")
        scanf("%d", &volume);
        printf("\n")

        cubes = findNb(volume)
        printf("The number of cubes is: %d", cubes)
}
